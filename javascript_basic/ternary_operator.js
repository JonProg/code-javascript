const life = 100;
const playerStatus = life <=0 ? 'Morto':'Vivo';

console.log(playerStatus);

// codicional ? caso true:caso false
// Uma simplificação interessante só que achei que tem muito simbolo e pouca
// sintaxe condicional tipo "if" e "else"